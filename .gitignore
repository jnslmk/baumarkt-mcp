.venv/
# Recreated by the python3 -> uv run hook on every verification run; not a
# deliberate lockfile (nothing pins dependencies), so never commit it.
uv.lock
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/
